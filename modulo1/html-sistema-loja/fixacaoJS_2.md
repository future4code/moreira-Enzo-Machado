```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  if (quantidade < 12) {
    let valorDaCompra = quantidade * 1.30
    return valorDaCompra
  } else {
    let valorDaCompra = quantidade * 1
    return valorDaCompra
  }
}
```